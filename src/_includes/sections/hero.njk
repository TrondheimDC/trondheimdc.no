<tdc-section id="hero" variant="hero">
  <div class="container">
    <div class="hero__content" data-animate="fade">
      <div class="hero__text">
        <h1 class="hero__title">
          {{ "hero.title" | t(lang) }}
        </h1>
        <p class="hero__date h3">
          {{ "hero.subtitle" | t(lang) }}
        </p>
        <p class="hero__tagline lead">
          {{ "hero.tagline" | t(lang) }}
        </p>
        <div class="hero__actions cluster">
          <a href="#tickets" class="btn btn--mascot btn--lg">
            {{ "hero.cta_tickets" | t(lang) }}
          </a>
          <a href="#cfp" class="btn btn--ghost btn--lg">
            {{ "hero.cta_cfp" | t(lang) }}
          </a>
        </div>
      </div>
      <div class="hero__mascot">
        <tdc-duck size="xl" animation="float" mood="happy"></tdc-duck>
      </div>
    </div>
    
    <div class="hero__scroll-hint" aria-hidden="true">
      <span class="animate-bounce">↓</span>
    </div>
  </div>
</tdc-section>

<style>
  .hero__content {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: var(--tdc-primitive-space-8);
    align-items: center;
    min-height: calc(100vh - 64px);
    min-height: calc(100dvh - 64px);
    padding-top: 64px;
  }

  .hero__title {
    margin-bottom: var(--tdc-space-stack-md);
  }

  .hero__date {
    color: var(--tdc-brand-primary-dark);
    margin-bottom: var(--tdc-space-stack-sm);
  }

  .hero__tagline {
    margin-bottom: var(--tdc-space-stack-lg);
    max-width: 50ch;
  }

  .hero__mascot {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero__scroll-hint {
    position: absolute;
    bottom: var(--tdc-primitive-space-6);
    left: 50%;
    transform: translateX(-50%);
    font-size: var(--tdc-primitive-font-size-2xl);
    color: var(--tdc-text-secondary);
  }

  @media (max-width: 768px) {
    .hero__content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero__mascot {
      order: -1;
    }

    .hero__actions {
      justify-content: center;
    }
  }
</style>
